[package]
name = "redbook"
description = "CDDA CD digital audio as per RedBook"
categories = ["command-line-utilities", "multimedia::audio", "hardware-support"]
keywords = ["ccda", "windows", "flac"]
authors = ["Mike Foster <https://github.com/MusicalNinjaDad>"]
repository = "https://github.com/MusicalNinjaDad/redbook"
license = "MIT"
edition = "2024"
version = "0.1.3"
exclude = [
    "/.vibe/plans",
    "/.vibe/.tmp",
    "/docs/**/*.json",
    "/graphify-out/cache",
]

[dependencies]
cdtoc = "0.14.0"
clap = { version = "4.6.5", features = ["derive"] }
errno = "0.3.14"
exit_safely = "0.3.1"
flacenc = { version = "0.5.1" }
humanize-duration = "0.0.7"
metaflac = "0.2.8"
musicbrainz_rs = { version = "0.14.0", features = ["sync", "native_tls"] }
reqwest = { version = "0.13.4", default-features = false, features = [
    "blocking",
    "native-tls",
    "json",
] }
serde_json = "1.0"
tabular = "0.2.0"
try_v2 = "0.10.1"
tracing = "0.1"
tracing-result = "0.0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "json"] }
windows-link = "0.100.0"
zune-jpeg = "0.5.15"

[dev-dependencies]
glob = "0.3.4"
proc-macro2 = "1.0.107"
rstest = "0.26.1"
similar-asserts = "2.0.0"
syn = {version = "3.0.5", features = ["full", "extra-traits"]}
tempfile = "3.27.0"
windows-bindgen = "0.100.0"

[build-dependencies]
build_safely = "0.5.4"
clap = { version = "4.6.5", features = ["derive"] }
clap_builder = "4.6.5"
clap_complete = "4.6.8"
clap_mangen = "0.3.0"

[workspace.metadata.ninja-xtask]
env = { "LD_LIBRARY_PATH" = false }

[lints]
# Explicitness
clippy.allow_attributes = "deny" # use expect instead
clippy.let_underscore_must_use = "deny" # use expect(unused_must_use, reason = "") instead
clippy.let_underscore_untyped = "deny"
clippy.unused_result_ok = "deny"
clippy.used_underscore_binding = "deny"

# Unsafe lints are documented at top of lib.rs for ease of auditing
